<%= form_for @docket do |f| %>


<% if @docket.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@docket.errors.count, "error") %> prohibited this docketfrom being saved:</h2>

          <ul>
            <% @docket.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>



<table class="condensed-table">
      <tr>
        <td><%= f.label :name %></td>

        <td><%= f.text_field :name %></td>
            
      </tr>

</table>
<br>
<br>

<%= f.submit "Submit" ,:style=>"margin:auto",:class=>"btn primary large"%>

<% end %>