<%= form_for @set do |f| %>


<% if @set.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@set.errors.count, "error") %> prohibited this setfrom being saved:</h2>

          <ul>
            <% @set.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>



<table class="condensed-table">
      <tr>
        <td><%= f.label :name %></td>

        <td><%= f.text_field :name %></td>
            
      </tr>

</table>
<br>
<br>

<%= f.submit "Submit" ,:style=>"margin:auto",:class=>"btn primary large"%>

<% end %>