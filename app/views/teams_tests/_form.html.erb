<%= form_for(@teams_test) do |f| %>
  <% if @teams_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teams_test.errors.count, "error") %> prohibited this teams_test from being saved:</h2>

      <ul>
      <% @teams_test.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :team_id %><br />
    <%= f.number_field :team_id %>
  </div>
  <div class="field">
    <%= f.label :test_id %><br />
    <%= f.number_field :test_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
